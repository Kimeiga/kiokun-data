The production deploy attempts to configure Cloudflare zone-level bot protection before deploying Pages. The existing deployment API token may not have permission to modify `/bot_management`; this must not block application deployment. The workflow therefore treats permission/authentication failures from the bot-protection configuration step as non-fatal while still surfacing the failure in the Actions log.
